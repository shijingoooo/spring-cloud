<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.demo.springcloud.alibaba.dao.OrderDao">

    <insert id="create" parameterType="Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_order(user_id,product_id,count,money,status)
        VALUE (#{userId},#{productId},#{count},#{money},0);
    </insert>

    
    <update id="update" >
        UPDATE t_order SET status = 1
        WHERE user_id = #{userId} AND status = #{status}
    </update>

    <resultMap id="BaseResultMap" type="com.demo.springcloud.alibaba.domain.Order">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <id column="user_id" property="userId" jdbcType="BIGINT"/>
        <id column="product_id" property="productId" jdbcType="BIGINT"/>
        <id column="count" property="count" jdbcType="INT"/>
        <id column="money" property="money" jdbcType="DECIMAL"/>
        <id column="status" property="status" jdbcType="INT"/>
    </resultMap>

</mapper>